<!-- edit データを更新するためのフォームを作成する -->
<h1>編集画面</h1>
<%= form_for(@list, url:"/todolists/#{@list.id}") do |f| %> <!-- DBから持ってきた(@list)をブロック変数fに渡している -->
    <!-- doとendの間に、「フォームオブジェクト」を作成 -->
    <!-- <.%で始まるタグは、Rubyで使われるタグ -->
    <h4>タイトル</h4>
    <%= f.text_field :title %> <!-- lists.rb(テーブルの設計図)に記入したカラムtitle -->

    <h4>本文</h4>
    <%= f.text_area :body %> <!-- lists.rb(テーブルの設計図)に記入したカラムbody -->

    <%= f.submit '保存' %>
    <!-- 「保存」ボタンを押すと、ルーティングで設定したとおり、todolistsコントローラのupdateアクションが呼び出される。 -->

<% end %>